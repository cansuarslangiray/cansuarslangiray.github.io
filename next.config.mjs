/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "",
    assetPrefix: '',
    output: "export",
    reactStrictMode: true,
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
